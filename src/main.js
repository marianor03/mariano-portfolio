import gsap from 'gsap';
import { ScrollTrigger } from 'gsap/ScrollTrigger';

gsap.registerPlugin(ScrollTrigger);

// Phase 2: pinned three-portrait hero (crossfade + scale/parallax scrub)
// and remaining section builds happen here.
